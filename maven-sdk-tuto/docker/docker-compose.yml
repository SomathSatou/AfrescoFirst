version: '3.4'
services:
#  Optional
#  maven-sdk-tuto-share:
#    image: ${docker.share.image}:${alfresco.share.version}
#    environment:
#      REPO_HOST: maven-sdk-tuto-acs
#      REPO_PORT: 8080
#    ports:
#      - "${share.port}:8080"
  maven-sdk-tuto-acs:
    image: alfresco-content-services-maven-sdk-tuto:development
    build:
      dockerfile: ./Dockerfile
      context: ../../../target
    environment:
      CATALINA_OPTS: "-agentlib:jdwp=transport=dt_socket,server=y,suspend=n,address=0.0.0.0:8888"
    ports:
      - "${acs.port}:8080"
      - "${acs.debug.port}:8888"
    volumes:
      - maven-sdk-tuto-acs-volume:/usr/local/tomcat/alf_data
    depends_on:
      - maven-sdk-tuto-postgres
  maven-sdk-tuto-postgres:
    image: postgres:9.6
    environment:
      POSTGRES_DB: alfresco
      POSTGRES_USER: alfresco
      POSTGRES_PASSWORD: alfresco
    command: postgres -c max_connections=300 -c log_min_messages=LOG
    ports:
      - "${postgres.port}:5432"
    volumes:
      - maven-sdk-tuto-db-volume:/var/lib/postgresql/data
  maven-sdk-tuto-ass:
    image: alfresco/alfresco-search-services:1.2.0
    environment:
      SOLR_ALFRESCO_HOST: maven-sdk-tuto-acs
      SOLR_ALFRESCO_PORT: 8080
      SOLR_SOLR_HOST: maven-sdk-tuto-ass
      SOLR_SOLR_PORT: 8983
      SOLR_CREATE_ALFRESCO_DEFAULTS: alfresco,archive
    ports:
      - "8983:8983"
    volumes:
      - maven-sdk-tuto-ass-volume:/opt/alfresco-search-services/contentstore
      - maven-sdk-tuto-ass-volume:/opt/alfresco-search-services/data
volumes:
  maven-sdk-tuto-acs-volume:
    external: true
  maven-sdk-tuto-db-volume:
    external: true
  maven-sdk-tuto-ass-volume:
    external: true